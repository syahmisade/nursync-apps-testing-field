{
  "name": "QuizAttempt",
  "type": "object",
  "properties": {
    "quizMode": {
      "type": "string",
      "description": "Quiz source type: category or set"
    },
    "quizKey": {
      "type": "string",
      "description": "Category key or set id string for this attempt"
    },
    "categoryKey": {
      "type": "string",
      "description": "Legacy/category attempt key. Matches QuizCategory.categoryKey."
    },
    "score": {
      "type": "number",
      "description": "Number of correct answers in this attempt"
    },
    "total": {
      "type": "number",
      "description": "Total number of questions in this attempt"
    }
  },
  "required": [
    "score",
    "total"
  ],
  "rls": {
    "create": {
      "created_by_id": "{{user.id}}"
    },
    "read": {
      "$or": [
        {
          "created_by_id": "{{user.id}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    },
    "update": {
      "$or": [
        {
          "created_by_id": "{{user.id}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    },
    "delete": {
      "$or": [
        {
          "created_by_id": "{{user.id}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    }
  }
}
